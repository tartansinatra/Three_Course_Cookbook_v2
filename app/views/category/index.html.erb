<!DOCTYPE html>
<html>
<head>
  <title>Categories</title>
</head>
<body>

<h2> List of Categories </h2>

<% for category in @categories %>
  <p>
   <%= category.name %>
   <%= link_to("Show Categories", category_path(category)) %>

</body>
</html>